{
  "lr": 0.00005,
  "optimizer": "adam",
  "max_epochs": 10,
  "early_stopping": true,
  "early_stopping_patience": 2,
  "early_stopping_delta": 0.05,
  "lr_scheduler": "cosine",
  "lr_scheduler_kwargs": {
    "warmup_steps": 2,
    "initial_learning_rate": 0.1,
    "decay_steps": 10
  }
}