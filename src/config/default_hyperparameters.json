{
  "lr": 0.00005,
  "optimizer": "adam",
  "max_epochs": 10,
  "early_stopping": true,
  "early_stopping_patience": 10,
  "early_stopping_delta": 0.002,
  "lr_scheduler": "plateau",
  "lr_scheduler_kwargs": {
    "mode": "min",
    "factor": 0.5,
    "patience": 5,
    "verbose": true,
    "threshold": 0.001
  }
}